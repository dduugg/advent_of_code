# typed: strict
# frozen_string_literal: true

require_relative '../../helper/solver'

class Y<%= year %>D<%= day %> < Solver
  sig { params(filepath: String).void }
  def initialize(filepath = 'input')
    super("<%= year %>/<%= day %>/#{filepath}")
  end

  sig { returns(Integer) }
  def part1
    0
  end

  sig { returns(Integer) }
  def part2
    0
  end
end

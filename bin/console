#!/usr/bin/env ruby
# typed: strict
# frozen_string_literal: true

require 'bundler/setup'
require 'pry-byebug'

Bundler.require

Dir[File.expand_path '2021/**/*.rb'].each{ require_relative(_1) }

Pry.start
